<%= form_for([@post, @post.comments.build]) do |f| %>
	<div class="field">
			<%= current_user.email %>
	</div>
	<div class="field">
		<%= f.text_area :body %>
	</div>
		<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
